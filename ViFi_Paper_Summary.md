# Vi-Fi 论文总结

> **Vi-Fi: Associating Moving Subjects across Vision and Wireless Sensors**
> 来源：ACM/IEEE IPSN 2022
> 作者：Hansi Liu, Abrar Alali, Mohamed Ibrahim 等（Rutgers University、CMU、Stony Brook 等多机构联合）

---

## 一、研究问题

**核心任务：** 将摄像头画面中视觉检测到的行人（边界框 Track ID）与其对应的智能手机设备标识符（MAC 地址等）进行**跨模态关联**。

```
摄像头视野中检测到的行人 BBX ←→ WiFi 信道中的手机 ID
```

**应用价值：**
- 多模态定位融合（RGB-D 深度 + WiFi 测距）
- 接触追踪 / 曝光通知（COVID 场景）
- 行人再识别与跟踪
- 车对行人（V2P）安全通信

**挑战：**
- 光照变化和遮挡导致外观特征不可靠
- 旁观行人（passersby）没有手机，增加基数不匹配问题
- 行人可随时进出摄像头视野，Track ID 会改变
- WiFi FTM 和 IMU 传感器存在多路径衰落、漂移噪声

---

## 二、Vi-Fi 数据集

### 2.1 采集背景

为评估视觉-无线关联任务，论文从零构建了一个**大规模多模态数据集**。

### 2.2 硬件设备

| 设备 | 型号 / 规格 | 采集内容 |
|------|------------|---------|
| RGB-D 摄像头 | Stereolabs ZED2 | 视频 10fps，深度 0.2~20m |
| 智能手机 | Google Pixel 3a | WiFi FTM（3Hz）、IMU（50Hz）、GPS（1Hz，仅户外） |
| WiFi 接入点 | Google Nest AP | 接收 FTM 测距信号 |

相机安装高度：2.4~2.8m；AP 置于相机旁边（室内挂顶/户外自行车把手）。

### 2.3 数据集构成

| 子集 | 场景 | 合法用户数 | 旁观者数 | 序列数 |
|------|------|-----------|---------|--------|
| Dataset A | 1 个受控室内办公室 | 5 人 | 0 | 15 |
| Dataset B | 5 个真实户外场所 | 2~3 人 | 1~9 人 | 75 |
| **合计** | **6 个场景** | — | — | **90 条，共约 162,000 帧** |

每条序列约 **3 分钟**；合法用户随身携带 Pixel 手机（不限拿法，可在手持或口袋中）。

### 2.4 传感器信号说明

| 信号 | 维度 | 含义 | 噪声特性 |
|------|------|------|---------|
| WiFi FTM | 1维（range r） | 手机到 AP 的距离，精度约1米 | 多路径衰落 |
| FTM std | 1维 | FTM 测量方差 | 反映多路径程度 |
| IMU | 9维 | 加速度计 + 陀螺仪 + 磁力计（各3轴） | 积分漂移 |
| GPS | 2维 | 经纬度（仅 Dataset B） | 1~4m 误差 |
| BBX | 4维 | 边界框 (x, y, w, h) | 遮挡时丢失 |
| Depth | 1维 | 相机到行人的深度（米） | — |

> **注：** OOSTraj 论文中使用 Vi-Fi 数据集的 **noisy GPS** 作为传感器轨迹输入，视觉 BBX 中心点作为视觉轨迹真值。GPS 在 Vi-Fi 原始任务中并非主要输入，而是 OOSTraj 针对性提取的信号。

### 2.5 标注方法

- 使用 ZED SDK 两遍追踪策略自动生成初始 BBX
- 每 10 帧人工核对并用 **VoTT（Visual Object Tagging Tool）** 修正
- 每位参与者分配匿名唯一 ID，人脸打码（用 ORB-HD/deface 工具）

### 2.6 数据配对性（回答你的问题）

**视觉轨迹和传感器轨迹是否配对？**

是的，这正是 Vi-Fi 数据集的核心设计：

> 每位合法用户（legitimate user）**同时**被摄像头追踪（视觉 BBX 序列）**并且**持有手机发送 FTM/IMU/GPS（传感器序列），通过**网络时间同步（NTP）** 对齐时间戳。

具体同步方式：
- 智能手机和相机通过网络时间协议（NTP）进行粗粒度时间同步
- 数据预处理时进行重采样与时间偏移校正（re-sampling + time offset between clocks）
- 对于旁观者，只有视觉 BBX，没有手机传感器信号（这也是关联任务的难点之一）

---

## 三、方法概述

Vi-Fi 提出两种关联方法：

### 方法一：亲和矩阵学习（Affinity Matrix Learning，主方法）

#### 网络结构（深度多模态架构）

```
相机模态输入                      手机模态输入
BBX坐标 (x,y) + 深度 d             FTM range/std + IMU (9轴)
形状: (N, k, 3)                    形状: (M, k, 11)
       ↓                                  ↓
  Bi-LSTM_c                          Bi-LSTM_p
  (2层, 输出32维)                    (2层, 输出32维)
       ↓                                  ↓
       └──────────── Feature Ensemble ────┘
                    (穷举所有 N×M 对，拼接64维向量)
                            ↓
                   3D Feature Cube Φ ∈ R^(N×M×64)
                            ↓
           Conv1(64→128) → BN → ReLU → Dropout
           Conv2(128→64) → BN → ReLU
           Conv3(64→32)  → ReLU
           Conv4(32→1)   → ReLU
                            ↓
              Affinity Matrix M ∈ R^(N×M)
                  (每个元素 = 一对 BBX-手机的关联概率)
```

#### 亲和矩阵的使用

- 对矩阵行做 **row-wise softmax** → 前向关联概率 A_r（手机→BBX）
- 对矩阵列做 **col-wise softmax** → 反向关联概率 A_c（BBX→手机）
- 追加额外行列，处理"无对应"情况（旁观者、视野外手机等）
- 推理时用**匈牙利算法**在 A_r 上求最优二部匹配

#### 损失函数（4项）

| 损失项 | 公式 | 作用 |
|--------|------|------|
| L_pc | 交叉熵（手机→BBX 方向） | 惩罚手机分配到错误BBX |
| L_cp | 交叉熵（BBX→手机 方向） | 惩罚BBX分配到错误手机 |
| L_cons | L1(A_r, A_c^T) | 正反方向一致性约束 |
| L_aff | 抑制非最大元素 | 稀疏化亲和矩阵 |
| **L** | (L_pc + L_cp + L_cons + L_aff) / 4 | 总损失 |

#### 部署策略

- **在线（Online）**：滑动窗口最近 10 帧（≈3秒），逐帧预测，2.8fps 实时
- **离线（Offline）**：滑动窗口投票（Consistency Voting），窗口长度 30 帧（≈10秒），多数票决定最终关联

---

### 方法二：二部图运动关联（Bipartite Motion Association，基线）

**不需要训练数据**，纯模型驱动。

#### 运动画像（Motion Profile）构建

每个实体（人或手机）的运动画像 `p = {j, φ, d}`：
- `j`：轨迹（2D 坐标序列）
- `φ`：朝向角（度）
- `d`：到参考点的距离（米）

| 模态 | 轨迹来源 | 朝向来源 | 距离来源 |
|------|---------|---------|---------|
| 视觉 | ZED 追踪器 BBX 中心点 | 相邻帧位移角 | RGB-D 深度值 |
| 手机 | IMU 步数积分（行人航位推算） | 磁力计重力轴旋转角 | WiFi FTM 测距 |

#### 相似度计算（3项）

| 相似度 | 方法 | 说明 |
|--------|------|------|
| I_φ（朝向） | DTW 动态时间规整 | 对比 φ_c 和 φ_p |
| I_j（轨迹形状） | DTW | 对比归一化运动轨迹 |
| I_d（距离） | FTM-深度匹配 | 补偿纯2D轨迹歧义 |

加权边得分：`E_uv = 0.1·I_φ + 0.1·I_j + 0.8·I_d`

用**匈牙利算法**最小化总加权和，得到最优 1:1 匹配。

---

## 四、实验结果

### 4.1 评估指标

**IDP（Identification Precision）**：
```
IDP = IDTP / (IDTP + IDFP)
```
衡量正确关联的边界框占所有关联边界框的比例（类似 Precision）。

### 4.2 性能对比

| 方法 | Dataset A 在线 | Dataset A 离线 | Dataset B 在线 | Dataset B 离线 |
|------|--------------|--------------|--------------|--------------|
| 二部图（仅FTM） | ~0.60 | ~0.65 | ~0.35 | ~0.40 |
| 二部图（仅IMU） | ~0.70 | ~0.75 | ~0.55 | ~0.60 |
| 二部图（FTM+IMU）| ~0.78 | ~0.83 | ~0.60 | ~0.68 |
| 亲和矩阵（仅FTM）| ~0.70 | ~0.78 | ~0.46 | ~0.50 |
| 亲和矩阵（仅IMU）| ~0.75 | ~0.82 | ~0.60 | ~0.72 |
| **亲和矩阵（FTM+IMU）**| **~0.81** | **~0.91** | **~0.81** | **~0.90** |

> 论文公布的总体结论：**81%（实时）到 91%（离线）** 的关联准确率

### 4.3 消融分析

| 分析项 | 结论 |
|--------|------|
| FTM vs IMU | FTM 捕获空间距离，IMU 捕获运动模式，两者互补 |
| 测量序列长度 | 序列 ≥ 3 秒（10帧）时准确率显著提升，达到 89% 中位数 |
| 投票窗口大小 | 窗口 30 帧（≈10秒）时离线准确率收敛 |
| 深度信息 | 补偿多人同向行走时的轨迹形状歧义 |

---

## 五、与 OOSTraj 论文的关系

| 方面 | Vi-Fi 论文 | OOSTraj 论文 |
|------|-----------|-------------|
| **任务** | 跨模态行人关联（视觉 ID ↔ 手机 ID） | 视野外轨迹预测（传感器轨迹 → 视觉轨迹） |
| **使用 Vi-Fi 数据的方式** | 原始任务（数据集作者本人） | 借用数据集，提取 GPS 作为带噪传感器轨迹 |
| **传感器输入** | FTM + IMU | noisy GPS（来自 Vi-Fi 的 GPS 字段） |
| **视觉数据** | BBX + 深度（训练关联） | BBX 中心点坐标（作为视觉轨迹真值） |
| **数据配对前提** | 是（同一用户同时有 BBX 和手机数据） | 是（继承 Vi-Fi 的配对设计） |
| **模型架构** | Bi-LSTM + 1×1 卷积亲和矩阵 | 多级 Transformer |

---

## 六、核心创新与局限

### 创新点

1. **首个无颜色依赖的视觉-无线关联系统**：仅依赖轨迹/运动/深度，不依赖外观颜色，适应标准服装/低光场景
2. **实时可行**：滑动窗口 10 帧（3秒），2.8fps 实时处理速率，端到端延迟 360ms
3. **大规模真实数据集**：90 条序列，含真实旁观者，开源（[Vi-Fi Dataset](https://sites.google.com/winlab.rutgers.edu/vi-fidataset)）

### 局限性

1. **亲和矩阵维度固定**：最大行人数 N_m 在训练时预设，无法动态扩展，需重新微调
2. **隐私问题**：摄像头与手机 MAC 地址关联可能引发隐私顾虑
3. **仅限 opt-in 用户**：旁观者不参与无线端，关联精度受旁观者密度影响
4. **室外 Dataset B 的二部图基线退化明显**：旁观者增多时手工特征不够区分
